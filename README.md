# ocr

### installation steps

####install python > 3.7

####install tesseract 4+

`brew install tesseract`

`brew install tesseract-lang`

####install pdftotext 

`pip install pdftotext`

####install pdfminer

`pip install pdfminer`


